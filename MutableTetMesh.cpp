#include "MutableTetMesh.h"

struct MutableTetMesh::ElementPool {
/*	std::queue<TetVertex*> dirtyVer;
	std::queue<Tetra*> dirtyTetra;
	std::queue<TetHalfFace*> dirtyHalfFace;
	std::queue<TetFace*> dirtyFace;
	std::queue<TetEdge*> dirtyEdge;*/
	//std::queue<Element*> 
};

void MutableTetMesh::addVertex() {
	//TetVertex *ver = m_pool->dirtyVer.empty();
}

void MutableTetMesh::addTetra() {

}

void MutableTetMesh::removeVertex() {

}

void MutableTetMesh::removeTetra() {

}